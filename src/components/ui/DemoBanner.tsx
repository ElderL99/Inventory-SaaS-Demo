export default function DemoBanner() {
  return (
    <div className="w-full bg-primary/10 border-b border-primary/20 px-6 py-2">
      <p className="text-xs text-primary">
        🚧 Modo demostración — datos simulados, funcionalidad limitada
      </p>
    </div>
  );
}
